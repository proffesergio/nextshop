# @nextshop/* shared packages are published PUBLICLY to npmjs.org.
# Public packages install with no auth. CI uses NODE_AUTH_TOKEN (an npm automation
# token) to publish — injected by actions/setup-node in the release workflow.
@nextshop:registry=https://registry.npmjs.org
